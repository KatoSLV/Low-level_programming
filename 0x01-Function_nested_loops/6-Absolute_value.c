#include "roadmap.h"

/**
* absVal - this program will computes the absolute value of an integer
* Return: return the same number of "num"
*@num: is the valor of current integer
*/

int absVal(int num)
{
	if (num < 0)
	{
		return (-num);
	}
	else
	{
		return (num);
	}
	return (0);
}
