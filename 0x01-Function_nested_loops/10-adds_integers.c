#include "roadmap.h"

/**
* addInt - add two integers
* Return: will return the addition of two int
* @a: is the first number to be add
* @b: is the second number to be add
*/

int addInt(int a, int b)
{
	return (a + b);
}
