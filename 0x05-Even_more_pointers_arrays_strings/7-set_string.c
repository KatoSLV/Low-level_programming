#include "roadmap.h"

/**
* set_string - will sets the value of a pointer to a char
* @s0: store the address of the value
* @s1: store the address of s0
* Return: Nothing
*/

void set_string(char **s1, char *s0)
{
	*s1 = s0;
}
