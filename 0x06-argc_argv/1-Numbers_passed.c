#include "roadmap.h"

/**
* main - will print the numbers of the arguments passed into it
* @argc: counter of arguments
* @argv: arrays of elements of the program
* Return: the numbers of arguments
*/

int main(int argc, char **argv __attribute__((unused)))
{
	printf("The numbers of arguments passed are: %d\n", argc);
	return (0);
}
