#include "roadmap.h"

/**
* mul - multiplies two integers
* @a: the number to be multiplied
* @b: the other nomber to be multiplied
* Return: return the value of a * b
*/

int mul(int a, int b)
{
	return (a * b);
}
