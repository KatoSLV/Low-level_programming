#include "roadmap.h"

/**
* isUpper - check if a character is uppercase
* @c: character to be checked
* Return: if upper return 1, 0 otherwise
*/

int isUpper(char c)
{
	return (c >= 65 && c <= 90);
}
